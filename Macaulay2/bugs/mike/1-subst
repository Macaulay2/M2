--*- M2 -*-

-- Mike, this is yours! FIXED.

k = GF(5,2,Variable=>a)
R = k[x,y,z,w]
p = map(k,R,{x=>a,y=>0,z=>0,w=>0})
p a
p x
p (a*x)
a*a
p (a*x-1)
a*a-1
assert( p (a*1_R) == a )
assert( p (a*x) == a*a )
assert( p (a*x-1) == a*a-1 )				    -- this one fails
