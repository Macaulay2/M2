Those options don't apply to this method!

oo22 = sort(List) -- sort a list
       *************************

       Synopsis
       ========

         * Usage: sort v
         * Function: "sort"
         * Inputs:
             * v, a list
         * Optional inputs:
             * DegreeOrder => ...,  -- specify Ascending, Descending, or null
             * MonomialOrder => ...,  -- specify Ascending or Descending monomial order
         * Outputs:
             * a list,  of the same length as v sorted in ascending order

