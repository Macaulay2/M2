documentation of core functions has to be redirected to Macaulay2Doc!

Function % Parser -- transform the value returned by a parser
Synopsis

    *

      Usage:
          f % p

    * Operator: %
    *
      Inputs:
          o f, a function (missing documentation )
          o p, a parser
    *
      Outputs:
          o a parser, a parser that feeds its tokens to p and filters the value returned by p through f

