    Macaulay2, version 1.3.1
    with packages: ConwayPolynomials, Elimination, IntegralClosure, LLLBases, PrimaryDecomposition, ReesAlgebra, SchurRings,
		   TangentCone

    i1 : load "~/src/M2-trunk/Macaulay2/packages/FormalGroupLaws.m2"

    i2 : universalFGLQ(3,"b","x","y")

			   2        2       2          2
    o2 = FormalGroupLaw{(4b  - 3b )x y + (4b  - 3b )x*y  - 2b x*y + x + y, 3}
			   1     2          1     2          1

    o2 : FormalGroupLaw

but

    + /home/dan/src/M2-trunk/BUILD/dan/builds.tmp/ubuntu32.production/StagingArea/i386-Linux-Ubuntu-9.10/bin/M2 --no-readline -q --print-width 125
    Macaulay2, version 1.3.1.1
    with packages: ConwayPolynomials, Elimination, IntegralClosure, LLLBases, PrimaryDecomposition, ReesAlgebra, SchurRings,
		   TangentCone

    i1 : load "~/src/M2-trunk/Macaulay2/packages/FormalGroupLaws.m2"

    i2 : universalFGLQ(3,"b","x","y")
    stdio:2:1:(3):[0]: error: destinations not specified for every generator, so expected original ring to be compatible with the ring of substitution values
