there seem to be two memory locations used for GC_free_space_divisor!

    (gdb) p foobar()
    $1 = (int *) 0x8460338
    (gdb) x/x 0x8460338
    0x8460338 <GC_free_space_divisor>:	0x00000032
    (gdb) p &GC_free_space_divisor
    $2 = (word *) 0x405a4750
    (gdb) p GC_free_space_divisor
    $3 = 3
    (gdb) list foobar
    182	
    183	int *foobar () { return &GC_free_space_divisor ; }
    184	
