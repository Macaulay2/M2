RR_100[x]
assert( (a = 2.) === .2p53e1 )
assert( (i = 2) === 2 )
-- assert( (quotientRemainder(x,a)) == (.5p100*x,0) )
-- assert( (quotientRemainder(a,x)) == (0,.2p100e1) )
-- assert( (quotientRemainder(x,i)) == (.5p100*x,0) )
-- assert( (quotientRemainder(i,x)) == (0,.2p100e1) )
-- assert( (x%a) == 0 )
-- assert( (a%x) == .2p100e1 )
-- assert( (x%i) == 0 )
-- assert( (i%x) == .2p100e1 )
-- assert( (x//a) === .5p100*x )
-- assert( (a//x) == 0 )
-- assert( (x//i) === .5p100*x )
-- assert( (i//x) == 0 )
assert( (x-a) === x-.2p100e1 )
assert( (a-x) === -x+.2p100e1 )
assert( (x-i) === x-.2p100e1 )
assert( (i-x) === -x+.2p100e1 )
assert( (x*a) === .2p100e1*x )
assert( (a*x) === .2p100e1*x )
assert( (x*i) === .2p100e1*x )
assert( (i*x) === .2p100e1*x )
assert( (x+a) === x+.2p100e1 )
assert( (a+x) === x+.2p100e1 )
assert( (x+i) === x+.2p100e1 )
assert( (i+x) === x+.2p100e1 )
assert( (x==a) === false )
assert( (a==x) === false )
assert( (x==i) === false )
assert( (i==x) === false )
assert( (x/a) === .5p100*x )
assert( (x/i) === .5p100*x )
