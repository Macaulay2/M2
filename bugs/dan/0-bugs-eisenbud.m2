-- Place bugs that you find in here, and
-- then check in the file, using (outside of M2):
--   cvs ci -m "another bug"

Tor should allow noncommutative rings (exterior algebra)

quotient should check to see if the second arg is a variable -- in this case it should always use the "linear" strategy

minimal resolutions over local rings.

time quotient(i3,S_0,Strategy=>Linear) Claims that the strategy is not implemented.

