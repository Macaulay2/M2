#! /bin/sh -e
prefix=$ENCAP_SOURCE/$ENCAP_PKGNAME
exec_prefix=$prefix
infodir="@infodir@"; eval infodir="$infodir"; eval infodir="$infodir"
cd "$infodir"
prefix=$ENCAP_TARGET
exec_prefix=$prefix
infodir="@infodir@"; eval infodir="$infodir"; eval infodir="$infodir"
bindir="@bindir@"; eval bindir="$bindir"; eval bindir="$bindir"
for i in *.info
do (set -x ; install-info --info-dir="$infodir" "$i")
done
if [ @DUMPDATA@ = yes ]
then (set -x ; "$bindir"/M2 --stop --dumpdata)
fi
