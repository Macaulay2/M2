
i1 : code methods use

o1 = -- ../../../Macaulay2/m2/methods.m2:188-190
     use HashTable := x -> (
          if x.?use then x.use x; 
          x)
     -- ../../../Macaulay2/m2/enginering.m2:101-106
     use Ring := R -> (
          if R.?ring then use R.ring;
          generators R;
          if R.?generators and R.?generatorSymbols then scan(R.generatorSymbols,R.generators,(sym,val) -> sym <- val);
          if R.?use then R.use R;
          R)
     -- ../../../Macaulay2/m2/packages.m2:276-279
     use Package := pkg -> if not member(pkg,packages) then (
          packages = prepend(pkg,packages);
          globalDictionaries = prepend(pkg.Dictionary,globalDictionaries);
          )

i2 : 
