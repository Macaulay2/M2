-- -*- M2 -*-

-- Load this package with 
-- 
--      	load "Foo.m2"
-- 
-- and it will fail, because the configuration is still a list and not a hashtable:

newPackage (
  "Foo",
   Configuration => {"path" => ""}
)
assert instance((options Foo).Configuration, HashTable)
