#include "SuffixTree.cpp"

auto LabelPool::prefix(Label &f, int indexOfPrefix) -> Label*
{
  
}

auto LabelPool::suffix(Label &f, int indexOfSuffix) -> Label*
{
  
}

auto LabelPool::insert(Label &f) -> Label*
{
  
}
