#! /bin/sh
DUMPDATA="@DUMPDATA@"
cd m2
ARCH=`uname -m`
export ARCH
if [ "$DUMPDATA" = yes ]
then echo Dumping data
     M2HOME=.. \
     ../libexec/Macaulay2 -silent -q '-e phase=1' \
	setup.m2 dumpdata.m2  '-edump()' '-e exit 0' \
    || exit 1
fi
