all :
	../../../../configure \
		--prefix=/usr/local

install :
	$(MAKE) -s install -o pre-install -o all prefix=/usr/local/encap ENCAP=yes

#		--enable-tarfile \
#		--enable-deb \
#		--enable-common-staging-area \
#		--enable-encap \

# Local Variables:
# compile-command: "make -f Makefile "
# End:
