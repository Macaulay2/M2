# ubuntu 32:

SSHPORT = 2227
RDPPORT = 3391

MACHINE = ubuntu32
RELEASE = 12.04
ARCH = i386
VERSION = $(RELEASE)-desktop

ISO = ubuntu-$(VERSION)-$(ARCH).iso
URL = http://mirror.anl.gov/pub/ubuntu-iso/CDs-Ubuntu/$(RELEASE)/$(ISO)

include ../Makefile.machine
