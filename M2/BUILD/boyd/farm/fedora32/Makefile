# Fedora 32:

SSHPORT = 2227
RDPPORT = 3391

OS = Fedora
MACHINE = fedora32
ARCH = i686

VERSION = 16
VERSION2 = Live-Desktop

ISO = Fedora-$(VERSION)-$(ARCH)-$(VERSION2).iso
URL = http://download.fedoraproject.org/pub/fedora/linux/releases/$(VERSION)/Live/$(ARCH)/$(ISO)

include ../Makefile.machine
