# debian 32:

SSHPORT = 2227
RDPPORT = 3391

OS = Debian
MACHINE = debian32
ARCH = i386

VERSION = 6.0.4
ISO = debian-$(VERSION)-$(ARCH)-netinst.iso
URL = http://ftp.utexas.edu/debian-cd/$(VERSION)/$(ARCH)/iso-cd/$(ISO)

include ../Makefile.machine
