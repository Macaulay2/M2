<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.15.0" xml:lang="en-US">
  <compounddef id="namespace_0d113074376267102137006052207234270101142105011074" kind="namespace" language="C++">
    <compoundname></compoundname>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para>An object that supports queries for divisors of a monomial.</para>
<para>This is an object for documentation purposes. Use the concrete implementations of this interface to get an actual DivFinder-like object.</para>
<para>DivFinder stores configurable <ref refid="struct_item_or_value" kindref="compound">Entry</ref> objects that represent a monomial together with any required additional information.</para>
<para>The class is parameterized on a Configuration. The Configuration class must have the following members.</para>
<para>A type <ref refid="class_monomial" kindref="compound">Monomial</ref> Represents a monomial.</para>
<para>A type <ref refid="struct_item_or_value" kindref="compound">Entry</ref> These are the things added to the finder. <ref refid="struct_item_or_value" kindref="compound">Entry</ref> represents a monomial along with any additional required information that client code needs to associate with monomials in the finder.</para>
<para>A type Exponent These are exponents of the monomials. Exponent must have a copy constructor, an operator= and an operator&lt; for comparision with other exponents.</para>
<para>A function Exponent getExponent(Monomial m, size_t var) const Returns the exponent of the variable var in m. m can be a const reference.</para>
<para>A function bool divides(Entry a, Monomial b) const A function bool divides(Monomial a, Entry b) const A function bool divides(Entry a, Entry b) const Returns whether a divides b. a and b can be const references. If <ref refid="struct_item_or_value" kindref="compound">Entry</ref> and <ref refid="class_monomial" kindref="compound">Monomial</ref> are the same type then only one function is needed.</para>
<para>A function size_t getVarCount() const Returns the number of variables. Variables are indexed from 0 to getVarCount() - 1. Must be a positive number.</para>
<para>static const bool UseDivMask Set to true to use div masks to speed up queries. This must be a static const data member.</para>
<para>size_t getUseAutomaticRebuild() const double getRebuildRatio() const size_t getRebuildMin() const If getUseAutomaticRebuild() returns true, the tree will call rebuild after a total of max(size() * getRebuildRatio(), getRebuildMin()) entry insertions and removals have occurred. A rebuild ratio of 0.5 and a minimum of 500 has worked well for random vectors.</para>
<para>A method bool isLessThan(Entry a, Monomial b) const A method bool isLessThan(Monomial a, Entry b) const A method bool isLessThan(Entry a, Entry b) const isLessThan must define a total order &lt; on entries/monomials. a and b can be const references. If <ref refid="struct_item_or_value" kindref="compound">Entry</ref> and <ref refid="class_monomial" kindref="compound">Monomial</ref> are the same type then only one function is needed.</para>
<para>It is possible to obtain non-const Entries from a DivFinder. It is allowed to change these, but the monomial they represent must not change. When the DivFinder calls some method X on the Configuration, the Configuration must not in return cause a method to be called on the DivFinder until the method X has returned. This is because the DivFinder may not be in a valid state at the time it is calling X.</para>
<para><xrefsect id="todo_1_todo000006"><xreftitle>Todo</xreftitle><xrefdescription><para>: put these docs somewhere else. </para>
</xrefdescription></xrefsect></para>
    </detaileddescription>
    <location file="submodules/mathic/src/mathic/DivFinder.h" line="69" column="1"/>
  </compounddef>
</doxygen>
