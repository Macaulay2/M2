VPATH = @srcdir@
all check reconfigure distclean clean :: ; @ set -e ; for p in @LIBRARIES@ ; do (set -x ; $(MAKE) -C $$p $@) ; done
Makefile : Makefile.in ; cd .. && ./config.status libraries/Makefile
clean :: ; rm -rf local
distclean :: ; rm -f Makefile
# Local Variables:
# compile-command: "make -C $M2BUILDDIR/libraries "
# End:
