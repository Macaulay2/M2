include ../config.Makefile
VPATH = @srcdir@
ifeq "$(encap)" "yes"

install :: $(target)/encapinfo
$(target)/encapinfo: encapinfo
	$(INSTALL_DATA) $< "$@"

ifeq "$(DUMPDATA)" "yes"
install :: $(target)/postinstall
$(target)/postinstall: postinstall
	$(INSTALL_PROGRAM) $< "$@"
endif

endif
